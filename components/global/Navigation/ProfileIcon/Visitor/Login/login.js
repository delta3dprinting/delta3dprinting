const profileDrawerLoginId = "login";

const profileDrawerLoginName = "Login";

const profileDrawerLoginMethod = () => {
  constructLoginModal();
};

const profileDrawerLoginObject = new ProfileDrawerObject(
  profileDrawerLoginId,
  profileDrawerLoginName,
  profileDrawerLoginMethod
);
