const profileId = "profile";

const profileName = "Profile";

const profileMethod = () => {
  profileInit();
};

const profileObject = new ProfileDrawerObject(
  profileId,
  profileName,
  profileMethod
);
