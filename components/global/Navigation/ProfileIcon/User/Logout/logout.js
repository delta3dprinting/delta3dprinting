const logoutId = "logout";

const logoutName = "Logout";

const logoutMethod = () => {
  console.log("Logout");
};

const logoutObject = new ProfileDrawerObject(
  logoutId,
  logoutName,
  logoutMethod
);
